/**
 * Legacy Seed Module (Supabase Mode)
 * Data initialization is managed automatically via Supabase PostgreSQL tables.
 */

export async function seedSampleData(): Promise<void> {
  // No-op for Supabase mode
}
